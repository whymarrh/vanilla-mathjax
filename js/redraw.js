$('.Message').livequery(function () {
	MathJax.Hub.Queue(["Typeset", MathJax.Hub]);
});

/*
$(document).ready(function () {
	$(".CommentButton, a.PreviewButton.TabLink").click(function (e) {
		MathJax.Hub.Queue(["Typeset", MathJax.Hub]);
	});
});
*/
